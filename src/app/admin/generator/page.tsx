import { GeneratorClient } from "@/components/admin/GeneratorClient";

export const metadata = { title: "Admin · Word Generator" };

export default function AdminGeneratorPage() {
  return <GeneratorClient />;
}
